---
const {title="Header"} = Astro.props;
import { Image } from "astro:assets";
const { imgSrc, ctaUrl } = Astro.props;
---

<section class="card-section">
  <div class="card-container">
    <div class="card">
      <h2> {title}</h2>
      <slot name="p1"/>
      <slot name="p1"/>
    </div>
    <div class="image">
        <Image src={imgSrc} alt="Card Image" fit="cover" style="width:100%;height:100%;object-fit:cover;object-position:center;position:absolute;top:0;left:0;z-index:1;" />
      
  </div>
</section>

<style>
  .card-section {
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    gap: 2rem;
    margin: var(--margin);
    padding: var(--padding);
    background-color: var(--color-accent);
  }
  .card-container {
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    gap: 2rem;
    width: 100%;
  }
  .card {
    background-color: rgb(0, 0, 0);
    padding: var(--padding);
    width: 50%;
    height: ;
    z-index: 100;
  }
  .card h2 {
    font-size: 3rem;
    margin-bottom: var(--margin);
  }
  .card p {
    font-size: 1.5rem;
    line-height: 1.5;
    margin-bottom: var(--margin);
  }
  .image {
    position: relative;
    width: 50%;
    height: 100%;
    min-height: 300px;
    overflow: hidden;
    border-radius: 10px;
  }
